<!-- Terminal-style ASCII separator with modern styling -->
<div class="ascii-separator-container">
  <!-- Sliding terminal window decoration -->
  <div class="terminal-window-decoration">
    <div class="window-controls">
      <span class="control-dot close"></span>
      <span class="control-dot minimize"></span>
      <span class="control-dot maximize"></span>
    </div>
    <div class="window-title">
      <span class="title-text">// system_info</span>
    </div>
  </div>

  <!-- ASCII separator with cyberpunk styling -->
  <div class="ascii-content">
    <div class="ascii-line">
      <span class="ascii-char">></span>
      <span class="ascii-char highlight">cat</span>
      <span class="ascii-char">/</span>
      <span class="ascii-char">section</span>
      <span class="ascii-char">;</span>
    </div>
    <div class="ascii-line">
      <span class="ascii-char">#</span>
      <span class="ascii-char highlight">--------------------------------------------------------------------------------</span>
      <span class="ascii-char">#</span>
    </div>
    <div class="ascii-line">
      <span class="ascii-char">></span>
      <span class="ascii-char highlight">echo</span>
      <span class="ascii-char">"</span>
      <span class="ascii-char glitch-text">[ ████████ ████ ] 64%</span>
      <span class="ascii-char">"</span>
    </div>
  </div>

  <!-- Sliding terminal cursor effect -->
  <div class="terminal-cursor-container">
    <span class="terminal-cursor"></span>
    <span class="terminal-cursor-text">[SYSTEM READY]</span>
  </div>
</div>

<style>
.ascii-separator-container {
  position: relative;
  margin: 3rem 0;
  padding: 2rem 0;
  font-family: 'Source Code Pro', monospace;
  font-size: 0.875rem;
  color: var(--color-text-muted);
  text-align: center;
}

/* Window decoration */
.terminal-window-decoration {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: linear-gradient(90deg, var(--color-gray-dark), var(--color-gray-medium));
  padding: 0.75rem 1.5rem;
  border-radius: 0.75rem 0.75rem 0 0;
  border: 1px solid var(--color-gray-medium);
  border-bottom: none;
  margin-bottom: 1rem;
}

.window-controls {
  display: flex;
  gap: 0.5rem;
}

.control-dot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
}

.control-dot.close {
  background: #ef4444;
  box-shadow: 0 0 8px rgba(239, 68, 68, 0.6);
}

.control-dot.minimize {
  background: #eab308;
  box-shadow: 0 0 8px rgba(234, 179, 8, 0.6);
}

.control-dot.maximize {
  background: #22c55e;
  box-shadow: 0 0 8px rgba(34, 197, 94, 0.6);
}

.window-title {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  font-size: 0.75rem;
}

.title-text {
  color: var(--color-accent);
  font-weight: 600;
  letter-spacing: 0.05em;
}

.ascii-content {
  background: linear-gradient(to bottom, var(--color-gray-dark), var(--color-gray-darker));
  border: 1px solid var(--color-gray-medium);
  border-radius: 0.75rem;
  padding: 1.5rem;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.4);
  position: relative;
  overflow: hidden;
}

.ascii-content::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(0, 191, 255, 0.1), transparent);
  animation: slide 3s infinite;
}

@keyframes slide {
  0% { left: -100%; }
  100% { left: 100%; }
}

.ascii-line {
  margin: 0.75rem 0;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  font-weight: 500;
}

.ascii-char {
  color: var(--color-text-muted);
  transition: color 0.3s ease;
}

.ascii-char.highlight {
  color: var(--color-accent);
  font-weight: 600;
  text-shadow: 0 0 8px rgba(0, 191, 255, 0.4);
}

.glitch-text {
  color: var(--color-success);
  position: relative;
  animation: glitch 2s infinite;
}

@keyframes glitch {
  0% { opacity: 1; }
  50% { opacity: 0.8; }
  100% { opacity: 1; }
}

/* Terminal cursor */
.terminal-cursor-container {
  margin-top: 1.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  font-size: 0.75rem;
  color: var(--color-accent);
}

.terminal-cursor {
  width: 8px;
  height: 16px;
  background: var(--color-accent);
  display: inline-block;
  animation: blink 1s infinite;
}

@keyframes blink {
  0%, 50% { opacity: 1; }
  51%, 100% { opacity: 0; }
}

.terminal-cursor-text {
  animation: pulse 2s infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 0.8; }
  50% { opacity: 1; }
}

/* Responsive design */
@media (max-width: 640px) {
  .ascii-separator-container {
    margin: 2rem 0;
    padding: 1rem 0;
  }

  .ascii-line {
    font-size: 0.75rem;
    flex-wrap: wrap;
    justify-content: center;
  }

  .terminal-window-decoration {
    padding: 0.5rem 1rem;
  }
}
</style>
